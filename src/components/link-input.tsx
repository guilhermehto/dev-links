import { Card } from './card';
import { Row } from './row';

type LinkInputProps = {};

export const LinkInput = () => {
  return (
    <Card variation="secondary" class="w-full">
      <Row spacing="between">
        <h4>Link #1</h4>
        Remove
      </Row>
      Platform
    </Card>
  );
};
